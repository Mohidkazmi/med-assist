# =========================================================================
# AI Medical Scribe Platform - Environment Variables Template
# Copy this file to `.env` and fill in the values for local development.
# =========================================================================

# --- PostgreSQL Settings ---
POSTGRES_DB=med_scribe_dev
POSTGRES_USER=scribe_admin
# WARNING: Always change the password in production environments!
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_PORT=5433

# --- pgAdmin Settings ---
PGADMIN_DEFAULT_EMAIL=admin@medscribe.com
# WARNING: Use a strong password for pgAdmin dashboard
PGADMIN_DEFAULT_PASSWORD=your_pgadmin_password
PGADMIN_PORT=5050

# --- Backend Settings ---
BACKEND_PORT=8000

# --- Frontend Settings ---
VITE_API_URL=http://localhost:8000/api/v1

# --- JWT / Authentication Settings ---
# Generate a strong secret key with: openssl rand -hex 32
JWT_SECRET_KEY=your_jwt_secret_key_here_change_before_production
# Signing algorithm — HS256 is recommended for single-server deployments
JWT_ALGORITHM=HS256
# Token lifetime in minutes (default: 30)
ACCESS_TOKEN_EXPIRE_MINUTES=30
